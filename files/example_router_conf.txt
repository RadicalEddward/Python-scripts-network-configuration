
en
conf t
if hostname specified:
    ho <host-name> 
for every interface:
    go to interface config mode
    set interface ip address + subnet mask
    do a not shutdown
if loopback specified:
    go to loopback interface config mode
    set ip address + subnet mask

=> don't forget new line!